演习
1.
wireshark可以捕获web浏览的三次握手。
见图http_tcp.jpg
2.
设置两个虚拟机为仅主机模式，确保IP在同一子网，他们之间可以ping通。
每个虚拟机默认设置是不可以ping主机得，通过网卡共享、网卡桥接等，可以实现虚拟机于主机相互访问。

3.
两个虚拟机配置为内部模式，设置为同一网络名称时，两虚拟机之间可以互相ping,虚拟机到主机不能ping。
在主机上运行wireshark，因为主机和虚拟机不在同一网络，所以看不到VM之间的流量。